# Large BIOS binary — store separately, not in git
Robin5.00

# binwalk extraction artifacts
_Robin5.00.extracted/

# iasl intermediate hex dump
acpi-tables/*.hex

# Compiled initrd override — regenerated by pstate-apply.sh
acpi-tables/acpi_override.cpio

# BC-250 BIOS ROM images (download from gitlab.com/TuxThePenguin0/bc250-bios)
BC250_*.ROM
